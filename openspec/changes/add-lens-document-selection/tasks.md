# Tasks: add-lens-document-selection

## 1. Arrival

- [x] 1.1 The delta is carried byte-identical from the openxFactory packet at
      `cb2d3a2c` (`sha256 48b9a60c…`, 8,363 bytes, `diff`-verified) and the
      openxFactory packet is closed as re-homed in the same window.
- [ ] 1.2 The capability `ideation-dashboard` reaches this corpus with
      `split-opendox-two-layer-product` § 5's shed. **Until it does, this change
      cannot archive** — `openspec validate --strict` passes and reports
      "Archive would refuse this delta: target spec does not exist".
- [ ] 1.3 This change's standing in THIS corpus is openDox's own act. The text
      arrives ratified in openxFactory (Brett, 2026-08-08) and `Status: draft`
      here until openDox ratifies it.

## 2. THE OWED NARROWING — the one thing this change does not do

- [ ] 2.1 **Narrow *A document selection drafts a staging-queue seed* to
      openDox's half.** The requirement carries both sides of the § 6.5 split in
      one sentence. openDox's half is the SELECTION and the drafted fragment's
      presentation; openxFactory's half is the DRAFTER and its route —
      `doc_health.staging_seed` and `/actions/staging-seed` — which RULING DQ-1
      keeps in openxFactory's own adapter, and which that repository owes to
      `split-opendox-two-layer-product` § 5.2a.
      **BLOCKED ON:** § 5.2a's successor capability id, which does not exist.
      openxFactory's adapter capability is unbuilt and `corpus-adapter-seam` is
      an unpromoted delta of the same packet, so the openxFactory half cannot be
      written against anything today and this half cannot be narrowed against a
      counterpart that has no name yet.
- [ ] 2.2 **Do not ratify requirement 2 in this corpus until 2.1 is done.**
      Ratifying it as it stands would have openDox ratify obligations about a
      `doc_health` drafter and a serve route that are openxFactory's.
      The other four requirements carry no such condition.

## 3. The realization that travelled with it

The openxFactory packet's §§ 1–3 and §§ 4.1–4.3 were built and ticked there; the
app-side code moves with the carve into `opensoft/openDox-code` rather than being
rebuilt, and the drafter does not move at all.

- [ ] 3.1 Re-verify the lens's views, the collapsed signature grid, the drafted
      panel's chrome and the theme-token guards against `openDox-code` after the
      carve lands there. The four-part floor proves test counts per destination
      rather than inheriting them.
- [ ] 3.2 Re-verify that the drafted-seed panel still reaches a drafter it does
      not own. After the carve the selection is openDox's and the drafter is
      openxFactory's adapter, so requirement 2's *"the response is TEXT and the
      path it belongs at"* becomes a call across the seam rather than a call
      within one process. **This is § 2.1's practical half and it lands with the
      adapter, not before it.**

## 4. What was still open when it moved, carried OPEN

- [ ] 4.1 **The first real staging seed is drafted and placed.** Carried open
      from the openxFactory packet's § 4.4 — *"Brett drafts and places the first
      staging seed from a real convergence, and the fragment gets its INDEX.md
      row"*. **Its three parts belong to three places** and it is carried with
      that stated: the SELECTION is openDox's, the DRAFTING is openxFactory's
      adapter under DQ-1, and the PLACING is a human act in openxFactory's own
      `ideation/staging/` — whose `INDEX.md` is openxFactory's file. This box
      therefore cannot be closed by openDox alone, and saying so is the point of
      carrying it.

## 5. What did NOT travel

- [x] 5.1 **`doc_health.staging_seed` and `/actions/staging-seed` STAY in
      openxFactory** (`split-opendox-two-layer-product` `tasks.md` § 6.5, RULING
      DQ-1). Recorded as a decision, not an omission. No byte of either is copied
      here, and requirement 2's drafting clauses are read as openxFactory's
      obligations until § 2.1 restates them.
