# Tasks: add-composed-view-authoring

## 1. Arrival

- [x] 1.1 The `## MODIFIED` block is carried byte-identical from the
      openxFactory delta at `cb2d3a2c` (`sha256 1754e5d3…`, 3,557 bytes) and the
      openxFactory packet is closed as re-homed in the same window.
- [ ] 1.2 The capability `ideation-dashboard` reaches this corpus with
      `split-opendox-two-layer-product` § 5's shed. **Until it does, this change
      cannot archive** — `openspec validate --strict` passes and reports
      "Archive would refuse this delta: target spec does not exist".
- [ ] 1.3 This change's standing in THIS corpus is openDox's own act. The text
      arrives ratified in openxFactory (Brett, 2026-08-08) and `Status: draft`
      here until openDox ratifies it.

## 2. The realization that travelled with it

The openxFactory packet's §§ 1 and 2 were built and ticked there; the code moves
with the carve into `opensoft/openDox-code` rather than being rebuilt.

- [x] 2.1 The capabilities route reports `repository`, resolved per request from
      the same authority a foreign-repository write is refused against, so the
      capability and the refusal cannot disagree.
- [x] 2.2 The unstripped probe and the declared repository reach the lens and the
      workbench's `openDraft`; every other surface keeps stripped capabilities.
- [x] 2.3 The create seed names the declared repository, never the project id.
- [ ] 2.4 Re-verify 2.1-2.3 against `openDox-code` after the carve lands there.
      They are ticked on the openxFactory evidence, which is the same code; the
      re-verification is owed because the floor's test counts are proven per
      destination, not inherited.

## 3. Verification

- [x] 3.1 Live from the merged `domains` project in openxFactory: the hand-off is
      offered and the workbench opens with the served repository as its context
      (it read the PROJECT id before, which the route would have refused).
- [ ] 3.2 **Brett creates the first real document from a project view.** Carried
      OPEN from the openxFactory packet, where it was the one unticked box — the
      write itself is his to make, not something to exercise against his checkout
      unasked.
